const {Pool} = require('pg')


//fill in the below information with you DB creds, host, etx
const pool = new Pool({
    user: '', 
    host: '',
    database: '',
    password: '',
    port: 0000,
})

modeul.exports = pool