export const POST_ALL_QUERY = 'https://cooperkyle.com/projects_api/blog_post/';
export const GET_SINGLE_POST = 'https://cooperkyle.com/projects_api/blog_post/';

// These are both the same now, instead we just append the id to the end of the
//   query, test this is working and then make changes were needded. 